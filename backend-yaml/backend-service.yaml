apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
  - port: 5050
    nodePort: 30000
    targetPort: 5050
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: backend
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP